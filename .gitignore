# Sigma Method (private — maps include noise, but the method stays local)
.sigma/

# Python
__pycache__/
*.pyc
*.pyo
*.egg-info/
dist/
build/

# Node
node_modules/
cli/dist/
cli/package-lock.json

# Models (too large for git) — but keep the production adapter committed
# so users get it on clone (~82MB, under GitHub's 100MB file limit).
# Use models/* (not models/) so git can descend into un-ignored subdirs.
models/*
!models/tsunami-adapter/
!models/tsunami-adapter/**

# Workspace (runtime artifacts)
workspace/.history/
workspace/notes/
workspace/plans/
workspace/deliverables/
workspace/*.txt
workspace/*.md

# OS
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/

# Claude
.claude/
CLAUDE.md
TSUNAMI.md
SCRATCHPAD.md
**/SCRATCHPAD.md

# Misc
*.log
/tmp/
intel/
workspace/
*.output
.vite/
PLAN.md
STATUS.md
PATTERNS.md
GAME_ENGINE_PLAN.md
ENGINE_STATUS.md
TRAINING_PLAN.md
logs/
BATTLE_PLAN.md
discord_bot/suggestions/
unsloth_compiled_cache/
aaron/

# Legacy (removed)
llama-server/
llama.cpp/

# Session artifacts / local operational files
INSTANCE_PROMPTS.md
