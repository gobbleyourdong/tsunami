# Sigma Method (private — maps include noise, but the method stays local)
.sigma/

# Python
__pycache__/
*.pyc
*.pyo
*.egg-info/
dist/
build/

# Node
node_modules/
cli/dist/
cli/package-lock.json

# Models — too large for git. Previously we committed the r=8 adapter at 82MB
# (under GitHub's 100MB limit), but r=32 + all-linear lands at 315MB. Users
# now train their own via training/train.py or pull from HuggingFace.
models/

# Per-machine binary symlinks (point at local build dirs under /tmp on Spark)
bin/llama-server
bin/sd-server

# Generated app outputs from agent runs — regenerable, not source
deliverables/

# Workspace (runtime artifacts)
workspace/.history/
workspace/notes/
workspace/plans/
workspace/deliverables/
workspace/*.txt
workspace/*.md

# OS
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/

# Claude
.claude/
CLAUDE.md
TSUNAMI.md
SCRATCHPAD.md
**/SCRATCHPAD.md

# ALL_CAPS filename convention: working files go in ALL_CAPS, research uses
# lowercase. Catches the whole class (SESSION_STATE, MORNING_BRIEFING, etc.)
# without per-file blocklists. Standards (README, LICENSE, CHANGELOG) and
# known canonical research (ADAPTER_PLAYBOOK, UNSLOTH_RECIPE) are whitelisted.
[A-Z][A-Z0-9_]*.md
!README.md
!LICENSE
!LICENSE.md
!CHANGELOG.md
!CONTRIBUTING.md
!CODE_OF_CONDUCT.md
# Tsunami skills are canonical workflow blueprints — track them
!tsunami/skills/**/SKILL.md
!tsunami/skills/**/WAVEFORM.md
# Tsunami cold-start scaffold — canonical onboarding for new instances
!tsunami/CLAUDE.md
# Repo-root pointer that orients AI agents at tsunami/CLAUDE.md
!/CLAUDE.md
# Scaffold catalog status — referenced from tsunami/CLAUDE.md as a cold-start
# resource. Without this exception the file gets caught by the ALL_CAPS
# convention above and a fresh clone won't have it.
!scaffolds/GAPS.md
# Note: retro-game scrape catalog lives at scaffolds/nudges/ (NOT inside
# scaffolds/.claude/) precisely so the .claude/ rule above doesn't strip
# it from clones. Git's negation pattern can't re-include a child if its
# parent dir is excluded.

# Misc
*.log
/tmp/
intel/
workspace/
*.output
.vite/
PLAN.md
STATUS.md
PATTERNS.md
GAME_ENGINE_PLAN.md
ENGINE_STATUS.md
TRAINING_PLAN.md
logs/
BATTLE_PLAN.md
discord_bot/suggestions/
unsloth_compiled_cache/
aaron/

# Legacy (removed)
llama-server/
llama.cpp/

# Session artifacts / local operational files
INSTANCE_PROMPTS.md

# sprite_pipeline generated output (OUTPUT_DIR pathing bug, see scaffolds/engine/tools/sprite_pipeline.py:32)
scaffolds/scaffolds/
