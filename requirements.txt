# TSUNAMI — Core runtime (the agent + server)
httpx>=0.24.0
pyyaml>=6.0
rich>=13.0
psutil>=5.9.0
Pillow>=10.0
ddgs>=9.0
websockets>=12.0
fastapi>=0.100.0
uvicorn>=0.20.0
# undertow QA tool — real browser screenshot + interaction
playwright>=1.40.0

# SPRITE EXTRACTION — scaffolds/engine/tools/pixel_extract.py
# Wavelet denoising is load-bearing: without it, sub-pixel color drift on
# AI-generated pixel art anchors k-means cluster centroids on fringe colors
# instead of letting them snap to the transparency palette entry. The scipy
# Gaussian fallback in _maybe_denoise is too weak to collapse that drift.
numpy>=1.24
scipy>=1.11
scikit-image>=0.22
scikit-learn>=1.3
PyWavelets>=1.5

# TRAINING — needed only for training/train.py + sweep.sh
# Install separately: pip install -r requirements-train.txt
# (kept out of core to avoid bloating tsunami-runtime installs)

# SYSTEM DEPS (NOT pip-installable):
#   - nodejs + npm           (L5 integration eval runs `npm` / `vite build`)
#   - chromium browser       (`playwright install chromium`)
#   - chromium shared libs   (`playwright install-deps chromium` on Ubuntu)
