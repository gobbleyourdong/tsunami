# Training Recipe

This project trains a model. See below for details.

## Requirements

- Python 3.10+
- PyTorch

## Running

```bash
python train.py --epochs 3
```

(train.py not included — deliberate bug for the probe to catch.)
