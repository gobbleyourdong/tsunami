# TSUNAMI — Training-only dependencies (Unsloth fine-tuning + sweep)
# Install: pip install -r requirements-train.txt
#
# These are heavy (torch, CUDA libs) — keep separate from runtime so
# `pip install -r requirements.txt` for tsunami-runtime stays light.

unsloth
transformers==5.5.0
trl
datasets
bitsandbytes
accelerate
peft
huggingface_hub
