# Overnight Crew Report — 2026-04-20

6-crew overnight campaign on Tsunami orchestration + scaffolds. 7-hour run.

## Ship counts

| Crew | Role | Ships |
|------|------|-------|
| Reef  | scaffold factory | 16 (6 CLI/web/infra/mobile + 2 cross-genre + 10 canary retrofits) |
| Tide  | probe factory   | 6 probes + 1 security patch (formally handed off to Kelp) |
| Kelp  | orchestration hardening | 16 orchestration/replay fixes including a meta-audit protocol |
| Shoal | ERNIE sprite pipeline   | 11 asset workflows (characters, tileable terrain, VFX, trees, portraits, autotile, items, UI) |
| Current | counter-propagating auditor | 28 attacks + 5 findings (2 sev-5 RCEs) |
| Coral | telemetry miner          | 4 work queues, 3 reports, 8 rounds |

## Headline work landed

- **Scaffold-first gate** — hard-gated `file_read` on inlined `data/*.json`, then hoisted the gate into `FileEdit / FileAppend / SummarizeFile / MatchGrep` (Kelp `b67b94d`, 109 tests across 11 replay suites). Pedagogical block message confirmed live in production session `1776743681`.
- **sev-5 cli_probe shell-injection RCE** — Current live-verified pre-delivery env-var exfil via malicious filename in `package.json::bin`; Tide patched by swapping `create_subprocess_shell` → `create_subprocess_exec` argv-list form (`17f29cc`).
- **Production-firing audit protocol** (Kelp `847fadb+e0b998b`, `scripts/crew/kelp/audit_production.py` + `fix_registry.jsonl`) — every fix now gets checked against real sessions before being marked landed.
- **6 new vertical probes shipped** — cli, mobile, training, infra, data-pipeline, docs. Dispatch registry grew 9 → 14 verticals.
- **11 asset workflows shipped** (Shoal) with canary artifacts and library seeds.
- **Advisory-vs-structural** surface closed by 4 structural conversions (Kelp) — drone-ignored system_notes converted to dispatch-level dedup short-circuits and force-deliver hooks, per sigma v7 "Convention Beats Instruction".

## Two meta-findings (sigma case-study candidates)

Both are instances of the same pattern — **commits claim a fix but the code never runs in production:**

1. **Dead scaffold-first predicate** — `project_init_gamedev.py:266` rewrote `package.json` name on provisioning, so the `gamedev-*-scaffold` marker (which 3 separate fixes relied on) was never present at runtime. 241 captured sessions with zero gate hits. Kelp found via round-10 mining audit. Fixed `0ee2632`.
2. **Outbound-exfil stub** — `tsunami/outbound_exfil.py` is a 29-line `return None` stub. Docstring acknowledges *"The real implementation was never committed."* Meanwhile, 12+ commits claim to add exfil-detection heuristics. Current found by reading the module. **Still open, sev 5.**

Both are clean sigma v7 "Formalization Exposes Drift" exemplars. Kelp's `audit_production.py` is the structural response — catches this class going forward.

## Outstanding at ramp-down

- 1 gap_queue item (`cross/kart_racer`)
- 3 open sev-5 pains: outbound_exfil stub, delivery-gate fail-open no-fingerprint, delivery-gate fail-open probe-exception
- `scaffold_first_toctou_scenes_evasion` (sev 4) — against Kelp's fresh predicate, mitigation is to anchor signal in provisioner-side session state
- `probe_shell_pattern_audit` — 7 probes likely share the RCE pattern; Kelp absorbed from Tide handoff
- ~12 smaller scaffold/probe pains (mobile_probe bypasses, docker_compose defaults, CSV injection, SSTI surface, etc.)

Coral's full state and breakdown lives at `~/.tsunami/crew/coral/report_2026-04-20.md`.
