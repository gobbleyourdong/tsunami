# tsunami CLAUDE.md — iteration log

Historical record of the cold-start scaffold doc's evolution. Lives next
to `CLAUDE.md` so the orientation doc itself stays trim. The "Modify
CLAUDE.md" instructions in the META-work section say to bump this log
on every doc change.

| Date | Iteration | What changed |
|---|---|---|
| 2026-04-26 | v1 | Initial cold-start scaffold. 21 scaffolds catalogued. Adventure-table for web/games/CLI. Three load-bearing primitives named. Deleted-module list. Refactor-in-flight note. |
| 2026-04-26 | v2 | TL;DR added. Work-type plans (refactor/replicator/research) called out as orthogonal to scaffold choice. Lighter-scaffolds note. Concrete answers for the cold-instance self-check. |
| 2026-04-26 | v3 | Aligned with the post-purge reality. "Major refactor in flight" section deleted (executed, not pending). "What was deleted" expanded to the full 6-commit / ~317-file / ~65K-line story. Added "What's actually here (current surface)" enumeration. Reframed agent-loop language ("you do the work, not a tsunami binary"). Updated cold-instance Q3 (no more lazy-import-in-agent.py since agent.py is gone — the question is now about historical references in stale docs). Added `core/<name>_probe.py` table for non-vision scaffolds. Removed `eddy.py` references (deleted in c94b029). |
| 2026-04-26 | v3.1 | Cold-tested with a fresh Explore agent. Four gaps fixed: (1) added explicit "if the genre isn't in the table, fall back to `gamedev/custom/`" note (the table establishes a `gamedev/<genre>/` pattern that breaks for metroidvania / rhythm-fighter / etc.). (2) Added explicit game-flow-vs-web-flow distinction: games use `genre_scaffolds/` + `core/gamedev_scaffold_probe.py`, NOT `style_scaffolds/` + `undertow_scaffolds/`. (3) Picked `gamedev_scaffold_probe.py` as canonical (the new data-driven flow) and explained why the older `gamedev_probe.py` exists. (4) Expanded the `skills/` entry from a one-liner to a real catalog with one-line trigger hints per skill (build-react / build-multi-page / build-recovery / in-place-cwd / iteration / qa-loop / visual-clone). Cold-test independent answers matched the doc's expected answers; v3.1 closes the remaining ambiguities. |
| 2026-04-26 | v3.2 | Second cold-test passed all 4 scenarios with exact ground-truth match (metroidvania flame-knight, brutalist photographer portfolio, broken vite-build recovery, vision_gate-fail-3-strikes). One minor v3.2 gap closed: Games table now opens with a one-line note pointing at `plan_scaffolds/gamedev.md` as the universal game-build plan (applies to every genre — covers data-driven scaffold flow + `data/*.json` editing + `vite build` emit). The 4 web-app rows have explicit per-row plans; the games rows did not. Now they do. |
| 2026-04-26 | v3.3 | Audited `skills/*/SKILL.md` and found 83 references to the deleted agent-tool API (shell_exec, file_write, file_read, file_edit, project_init, generate_image, match_glob, message_chat, message_result). The skills are written for the dead tsunami-agent loop's tool registry; their workflow logic is still correct but the tool-call syntax is historical. Added a tool-API translation table to the skills section: `shell_exec` → Bash, `file_read` → Read, `file_write` → Write, `file_edit` → Edit, `match_glob` → Glob, `project_init` → no direct equivalent (copy + customize), `generate_image` → no direct equivalent (image-gen deferred), `message_result` → just respond in text, `message_chat` → NEVER (anti-pattern). Less invasive than rewriting 7 skill files; cold instance reads CLAUDE.md, sees the table, can interpret skills correctly. WAVEFORM.md mention removed from the skills bullet (no skills currently have one — the gitignore exception is precautionary). |
| 2026-04-26 | v3.4 | Iteration-14 audit caught a major mis-categorization. `mesh/` (21 files / 392 KB) was kept across iterations 1-13 as "game-rendering 3D mesh utilities" based on the directory name. Reading its README revealed it's actually **MegaLAN — a decentralized compute mesh** for distributing tsunami agent jobs across networked nodes (P2P, identity / peer / ledger / discovery layers). With tsunami not invoked anymore + no agent jobs to distribute, the entire subsystem is dead orchestration infrastructure. Nuked the dir; verified no external dependents in the surviving codebase. CLAUDE.md mentions of `mesh/` removed from "What's actually here" + "Files NOT to touch" sections. Also short-circuited `tools/riptide.py` vision grounding (same pattern as `seed_from_image.py` in iteration 13 — was iterating through 3 dead localhost endpoints, now returns a deferred-error immediately with a re-enable hint pointing at Claude vision API). All 13 `core/<name>_probe.py` files audited and verified clean (zero broken imports, zero dead-endpoint refs, all import-test successfully). |
| 2026-04-26 | v3.5 | Iteration-15 systematic docstring sweep across all surviving files (applying iteration-14's lesson: don't trust dir/file names). Caught one more mis-categorization: `target_layout.py` was described in CLAUDE.md across multiple sections as "extracts UI element bboxes from a reference image and returns ratio-based CSS" — this was wrong. The actual file is "Full-page ERNIE target-layout GENERATION" — calls the deleted ERNIE-Image endpoint at `localhost:8092` to GENERATE a reference image (the bbox EXTRACTION half is `tools/riptide.py`, which was already short-circuited in iter 14). Both halves of the visual-clone pipeline are now dormant until image-gen returns. Nuked `target_layout.py` (zero external imports — `vision_gate.py` only used the name as a parameter for a Path). Updated 4 CLAUDE.md mentions: removed from "Top-level patterns" enumeration, replaced the honorable-mention bullet with `routing.py` (was missing from that list anyway), updated `visual-clone` skill description to flag it as currently degraded, updated cold-instance test answer #2 to point at responsive flex/grid CSS instead of the dormant bbox helper. |
| 2026-04-26 | v3.6 | Iteration-16 caught dead-endpoint code in the LOAD-BEARING patterns themselves (vision_gate.py + undertow.py — the files the choose-your-own-adventure points cold instances at). Three short-circuits applied with consistent pattern: early-return + clear deferred message + preserved-below-for-future-reactivation: (1) `vision_gate.vision_check()` was POSTing to `localhost:8090/v1/chat/completions` and **returning `passed=True` on endpoint error (fail-open!)** — silently passing every build. Changed to fail-closed: returns `passed=False` with explicit "wire to Claude vision API to re-enable" issues string. This is a real correctness fix, not just a hygiene fix. (2) `undertow._vlm_describe_screenshot()` was POSTing to `BEE_ENDPOINT/v1/chat/completions` (Qwen3.6/Gemma) — short-circuited to `None` (caller's `_describe_screenshot()` pixel-stats fallback handles it). Saves 120s per call. (3) `undertow._eddy_compare()` similarly short-circuited to "UNCLEAR: VLM compare deferred". Saves 20s per call. The Playwright lever-pulling machinery in undertow.py (screenshots, keys, clicks, console reads via `_lever_*` functions) is unchanged and works fine — only the VLM-augmented helpers are dormant. |
| 2026-04-26 | v3.7 | Iteration-18 META-orientation cold-test (validating that an instance asked to MODIFY tsunami — not USE it — can self-orient). The cold-test agent reverse-engineered all 4 conventions correctly (probe registration via `core/dispatch.py`'s `_PROBES` dict, style-scaffold's strict 1:1 pairing with `undertow_scaffolds/<name>.md`, SKILL.md's required `## When` / `## Pipeline` / `## Gotchas` sections, iteration-log bump convention) — but had to GUESS at the dispatch.py registry pattern by reading code. Closed the gap with a new "Adding new probes / styles / skills (META work)" section directly above "Where to dig deeper". The section walks through 5 META operations explicitly (add probe / add style / add skill / modify CLAUDE.md / modify a scaffold) with the actual file paths, line numbers, and required updates. Verified `dispatch.py` has `_PROBES = {...}` at line ~49 and self-errors with "Add one or rename the scaffold" if the dispatch can't find a probe — that's what makes the dispatch.py update step feel safe to a cold instance. Verified `style_scaffolds/manifest.py` is on-demand auto-discovery (no manifest update needed for new styles). Verified SKILL.md format consistency across 3 sample skills. |
| 2026-04-26 | v3.8 | Iteration-19 validation cold-test (re-running iteration-18's META scenarios with v3.7 in place + a doc-only constraint, no code peeking). All 4 scenarios passed from doc-content alone, zero gaps found, zero code-reading required. Verbatim verdict from the cold-test agent: "META section is complete and production-ready. Ready for independent cold instances." Iteration-20 hygiene cleanup: moved this iteration log out of CLAUDE.md (was accreting to ~2000 words at the bottom — historical noise that didn't help orientation) into this sibling CHANGELOG.md. CLAUDE.md retains a one-line pointer at the same anchor. The orientation doc trims down by ~1000 lines of prose; the historical context lives one click away. |
| 2026-04-26 | v3.9 | Iteration-22 stale-perimeter-doc audit (extending iteration-21's lesson). Audited `scaffolds/GAPS.md` and found 6 stale claims: (1) 3 scaffolds still in "Open" rows whose `GAP.md` files were deleted (per the file's own convention, that means closed); (2) "all three need a bespoke gate script" — they have one each (`openapi_probe`, `extension_probe`, `electron_probe`, all in `core/`); (3) "Shared inference server — contention rules" section referenced retired `:8090/:8092/:8095` Qwen3.6/ERNIE endpoints; (4) "Live-inference runs (`python3 -m tsunami.cli`)" — `tsunami.cli` was deleted in c94b029; (5) `~/.tsunami/inference.lock` advisory lock — no inference happens anymore; (6) `planfile.py::_DOMAIN_SIGNALS` — `planfile.py` was deleted in c94b029. Rewrote GAPS.md from "multi-instance rollout coordination" framing to "catalog-status reference" framing — simpler scope matches current reality (no parallel sigma instances per scaffold; agent harnesses run in their own process and call patterns directly). Updated CLAUDE.md "Where to dig deeper" pointer at GAPS.md from "(10 closed, 3 open)" to "(13 scaffolds, all closed)". |
| 2026-04-26 | v3.10 | Iteration-23 third-layer perimeter audit. Two findings: (1) `scaffolds/engine/README.md` line 104 referenced the deleted `tsunami/agent.py` and `error_fixer.fix_design_validation_errors` (also deleted). Cleaned both refs to point at AI-agent-harness language + pointer to CHANGELOG.md for the auto_fix retirement story. (2) `scaffolds/.claude/nudges/` (71 retro-game dirs / 2053 JSON+MD files / 1.5MB total) was gitignored by the `.claude/` catch-all rule. Same problem as iter-22's GAPS.md — CLAUDE.md was pointing cold instances at a directory that didn't ship with a clone. The user explicitly flagged retro-game scrapes as preservation-target ("nothing is lost"). Added `!scaffolds/.claude/nudges/` exception (plus `**` recursion). Verified all 2053 files are .json/.md, none >100KB, no sensitive content (public-domain pixel-art generation prompts for retro games). CLAUDE.md "Files NOT to touch" mention of nudges updated with note about the explicit exception. Note: ~30+ other audit/spec/inventory files in `scaffolds/.claude/` (CLAUDE_SYSTEMS_AUDIT, COMPONENT_ESSENCE_MAP, GAMEDEV_FRAMEWORK_PLAN, etc.) plus subdirs (blockouts/, character_builders/, combat/, game_essence/, sprite_sheets/, lovable_scrape/, seed_data/, tools/) are STILL gitignored. Out of scope for auto-commit — would need user direction on which to ship. |
| 2026-04-26 | v3.11 | Iteration-23 fix-up: the iter-23 attempt to ship `scaffolds/.claude/nudges/` via a `!scaffolds/.claude/nudges/` gitignore exception didn't work — git's negation pattern can't re-include a child whose parent dir is excluded ("It is not possible to re-include a file if a parent directory of that file is excluded"). The broader `.claude/` rule on line 46 won. Cleaner fix: moved `scaffolds/.claude/nudges/` → `scaffolds/nudges/`. New path isn't gitignored (no `nudges/` rule anywhere), so it ships cleanly. The 71 retro-game scrape dirs / 2053 .json+.md files / 1.5MB now live one directory up. CLAUDE.md "Files NOT to touch" + the in-line games-table reference both updated to the new path. .gitignore: removed the broken `!scaffolds/.claude/nudges/` exceptions, replaced with a comment explaining the parent-dir gotcha so future maintainers don't accidentally try the same broken pattern again. |
| 2026-04-26 | v3.12 | Iteration-29 numeric-claim audit caught a count error that survived the entire session. CLAUDE.md (root) + README.md both claimed "21 scaffolds" — actual count is 20. The "21" was wrong from day 1 (iter 1's CLAUDE.md miscount). Coincidentally became numerically right when iter 23 added `scaffolds/nudges/` (the retro-game scrape catalog moved out of `.claude/`), but semantically wrong because nudges is NOT a scaffold — it's a reference catalog. Fixed both docs to "20 scaffolds + sibling `scaffolds/nudges/` retro-game reference catalog". The original v1 row in this CHANGELOG file still says "21 scaffolds catalogued" — preserved as historical record (Maps Include Noise) since it documents what was claimed at the time, not what's true now. |
| 2026-04-26 | v3.13 | Iteration-30 systematic numeric-claim sweep (extending iter-29's lesson). Cross-checked every numeric claim across CLAUDE.md / README.md / GAPS.md against actual `ls`/`find` counts. Found SIX more mismatches: (1) `style_scaffolds/` claimed 9, actual 10 (shadcn_startup + swiss_modern were added but never made it into the visual-style table); (2) `undertow_scaffolds/` claimed 9, actual 17 — the doc treated style/undertow as strict 1:1 pairing, but 7 undertows are CROSS-CUTTING QA categories (web_polish, vision_analysis, bug_finding, brand_consistency, art_direction, game_feel, sprite_quality) that apply across multiple styles; (3) `genre_scaffolds/` claimed 18, actual 17; (4) `nudges/` claimed 71 game dirs, actual 69; (5) `core/` claimed "17 files" without distinguishing the 13 actual probes from the 4 supporting modules (dispatch + _probe_common + __init__ + the gamedev_scaffold_probe); (6) GAPS.md claimed "13 scaffolds, all closed" — actual 20 (the file was missing the entire `gamedev/` row from its catalog enumeration). Big structural fix: documented the 7 cross-cutting undertows as their own table in the visual-style section, dropped the false "1:1 pairing is strict" claim from the META section. shadcn_startup + swiss_modern added to the visual-style table with explicit notes that they don't have same-name undertows. GAPS.md catalog updated to include `gamedev/` row + count corrected to 20. CLAUDE.md "Where to dig deeper" pointer at GAPS.md updated to "(20 scaffolds, all closed)". README.md "eight visual styles" → "ten" with the same cross-cutting undertows note. Lesson: structural claims (paths, conventions, file presence) drift differently from numeric claims (counts, ratios) — both audits needed; the cold-test agents validated row-by-row content but never cross-checked enumeration against actuals. |
| 2026-04-26 | v3.14 | Iteration-31 verification of iter-30's META section claims (every "REQUIRED" / "MUST" / "always" / "1:1" assertion). Verified: (1) all 10 style_scaffolds have the required `default_mode` + `applies_to` + `mood` frontmatter — accurate; (2) all 7 cross-cutting undertows I named in iter-30's new table actually exist as files — accurate; (3) `shadcn_startup.md` + `swiss_modern.md` exist — accurate. Found ONE more structural drift: iter-18's "SKILL.md REQUIRED: ## When + ## Pipeline + ## Gotchas" claim — `build-recovery/SKILL.md` uses `## Pattern → Action` instead of `## Pipeline` (a decision-table workflow rather than sequential). The conceptual role is the same (it's the workflow section). Softened the META requirement to "a workflow section — usually `## Pipeline` for sequential skills, but `build-recovery` uses `## Pattern → Action` (decision table) instead." Pattern from iter-29/30/31: claims that say "REQUIRED" or "1:1" or "always" tend to be aspirational descriptions of the most-common case, not strict invariants. Worth softening to honestly reflect variation. |
| 2026-04-26 | v3.15 | Iteration-32 — extending iter-31's "REQUIRED claims drift" pattern to gate-cascade claims. CLAUDE.md said "verify by following the gates pattern in `deliver_gates.py`" and listed 6 gates (code_write, asset_existence, prop_type, runtime_smoke, vision_gate, accessibility_gate). Reality: `deliver_gates.py::DELIVER_GATES` registry has only **2** gates wired (`code_write_gate`, `asset_existence_gate`). The other 4 are either in adjacent modules (`vision_gate.py` is its OWN module, not a deliver_gates function), executed directly by the agent (tsc + Bash for prop_type, Playwright via undertow.py for runtime_smoke), or aspirational (`accessibility_gate` was already marked "future"). Reframed the cascade list to honestly distribute the checks across their actual locations: 2 in `deliver_gates.py`, 2 run via your own Bash, 1 in `vision_gate.py` (currently dormant), 1 future. Same lesson as iters 30-31: doc claims that group-by-conceptual-pattern need to disclose where each piece actually lives, not pretend they're all in one module. |
| 2026-04-26 | v3.16 | Iteration-33 — extending the doc-claims-vs-actual-content audit (iter 30/31/32 pattern) to the load-bearing pattern files themselves. Verified: (1) `circulation.py` matches the doc claim EXACTLY (`flowing → eddying → probing → broken` state machine, threshold default 3, on_eddy/on_trip callbacks — all there). Doc accurate. (2) `undertow.py` HAS `QAReport` class as claimed, but the doc undersells the lever surface. Doc named 4 levers ("screenshots, presses keys, clicks elements, reads the console"); actual is 9 async lever kinds (`screenshot`, `press`, `click`, `type`, `read_text`, `motion`, `sequence`, `autopilot`, `ghost_classes`) + 2 static variants (`ghost_classes_static`, `unused_dep_static`). Plus the doc listed "console" as a lever — actually console errors are a SIDE-CHANNEL passed through `console_msgs`, NOT a separate lever. Plus the doc said levers "return a structured QAReport" — actually each lever returns `LeverResult`; `pull_levers()` aggregates them into a `QAReport`. Updated the undertow.py bullet to enumerate all 9 lever kinds, clarify the console-as-side-channel point, and distinguish `LeverResult` from `QAReport`. |
| 2026-04-26 | v3.17 | Iteration-34 — extending the doc-claims-vs-actual-content audit to outbound_exfil.py. Doc said it blocks "literal API keys / private keys / exfil-shaped URLs" — accurate but undersells. Reality: 4 detector categories — (1) credentials (Anthropic/OpenAI/GitHub/Slack/Google API keys + JWT-shaped tokens), (2) PEM private keys, (3) env-var-value leaks (when ≥12-char env value appears in written content — UNDOCUMENTED in CLAUDE.md), (4) URLs to known webhook-capture services (narrow high-confidence list: webhook.site, requestbin etc.; NOT all webhooks — Slack/Discord/pipedream URLs PASS because they have legitimate uses). Updated the bullet to enumerate all 4 categories explicitly + name the entry point signature `check_outbound_exfil(content, filename, task_prompt) → Optional[str]` so a cold instance knows the exact API to call. Plus the deliberate-narrowness on URL detection is now documented (was previously implied by "high-confidence" framing in the docstring but not surfaced to cold-start readers). vision_gate.py separately verified clean — its CLAUDE.md description is conceptual and accurate to the file's purpose (no false count claims like undertow had). |
| 2026-04-26 | v3.18 | Iteration-35 — CRITICAL find. `core/dispatch.py` was broken at import-time for 21 iterations because of iter-14's nuke of `core/gamedev_probe.py`. dispatch.py:34 still had `from .gamedev_probe import gamedev_probe`; dispatch.py:91 had `_gamedev_probe_legacy = gamedev_probe`; gamedev_scaffold_probe.py:251 had a lazy `from .gamedev_probe import gamedev_probe` for the legacy fall-through path. None of my prior iter-14, 15, 17, 24, 25, 26, 28, 32 cold-tests / smoke-tests caught this because they imported probes individually (cli_probe, mobile_probe, etc.) — never dispatch.py. The CLAUDE.md META section's "register in core/dispatch.py's `_PROBES` dict" instruction relied on a file that errored before reaching `_PROBES`. Three fixes: (1) dispatch.py:34 — removed the broken import, replaced with a comment explaining the legacy probe was retired; (2) dispatch.py:91 — `_gamedev_probe_legacy = None` (was assigning the broken import); (3) gamedev_scaffold_probe.py:251 — replaced the lazy legacy-fall-through import with a `skip()` returning a clear "legacy probe retired 2026-04-26" message. Smoke-tested: dispatch imports cleanly + all 14 probes route correctly + gamedev fall-through returns skip without crashing. Lesson: file-level smoke tests don't catch transitive-import breakage. The dispatch.py case shows that an iteration's deletion can break a sibling file's imports without the iteration noticing — and 4 cold-test passes can pass without the breakage surfacing if no test exercises the importer. Worth checking dispatch-style indexing files as a class. |
| 2026-04-26 | v3.19 | Iteration-37 — end-to-end smoke-tested the scaffold-pick functions (which iter-8 routing.py resurrection was supposed to fix). Results: `pick_style` works (matched "brutalist landing page" → `brutalist_web`); `pick_genre` works (matched "metroidvania flame knight" → `metroidvania.md`); `pick_game_replica` works correctly but has a content-coverage gap — `_GAME_SIGNALS` only has Castlevania: SotN (1997) entry, no NES Castlevania (1986) entry, despite `nudges/1986_castlevania/` having sprite scrapes. Adding signals is curation work (you don't want "castlevania" too broad), flagged not auto-fixed. Also caught: `scaffolds/<name>/scaffold.yaml` (auto-generated drone-facing prop contract from TS interfaces) only existed for `react-app`; 8 other closed scaffolds were missing it. Regenerated 6 (dashboard, landing, data-viz, form-app, fullstack, realtime) successfully. 2 failed (auth-app, ai-app) — they don't have the assumed `src/components/ui/` directory structure that `regen_scaffold_yaml.py` expects; their UI lives elsewhere. The scaffold.yaml is technically vestigial (only the regen script references it; no runtime code consumes it), but per Maps Include Noise + the regen-tool-exists-for-a-reason argument, generating the missing files is small concrete improvement at zero downside cost. |
| 2026-04-26 | v3.20 | Iteration-38 fix-up for iter-37's auth-app + ai-app scaffold.yaml regen failures. Investigation: both scaffolds inherit UI components from react-app (auth-app adds `useAuth.tsx` + `ProtectedRoute.tsx`, ai-app adds `useChat` + chat-stream wiring) — they don't have self-contained `src/components/ui/` directories that the regen script's hardcoded path expected. Updated `regen_scaffold_yaml.py` to handle this case gracefully: when no ui dir is found, emit a small "inheritance-marker" scaffold.yaml that documents `inherits_from: react-app` + points at `__fixtures__/drone_natural.tsx` for the scaffold-specific contract. Both auth-app and ai-app now have scaffold.yaml files (~370 bytes each — much smaller than the 8KB self-contained ones, but explicit about inheritance). All 9 closed-vision-gated scaffolds now have a scaffold.yaml. |
| 2026-04-26 | v3.21 | Iteration-39 — surfaced the new (since iter 37/38) scaffold.yaml infrastructure in CLAUDE.md. Added a bullet in "Where to dig deeper" describing scaffold.yaml as the auto-generated component prop contract, noting which 9 scaffolds have it (the React-shape vision-gated ones), explaining the inheritance-marker pattern (auth-app + ai-app → `inherits_from: react-app`), and naming the 11 scaffolds without (bespoke-gate / games / lighter — different contract models). The regen command is also documented inline. Cold instance reading the doc now knows scaffold.yaml exists, when to consult it (React-shape scaffolds), when it doesn't apply (bespoke-gate / games / lighter), and how to regenerate it after a UI change. Closes the iter 37+38 architectural gap (scaffold.yaml existed but wasn't in the cold-start orientation surface). |
| 2026-04-26 | v3.22 | Iteration-40 — caught the EXACT failure mode `regen_scaffold_yaml.py`'s docstring warned about: "If [scaffold.yaml] drifts from the actual TS types, drones hallucinate props that cause build failures." `react-app/scaffold.yaml` had drifted significantly (97 lines changed when regenerated against current TS source). Many components had grown new props in TS that weren't in the contract: `Accordion` (+type/defaultOpen/collapsible), `Avatar` (+fallback/alt/shape/radius/status), `Button` (+loading/fullWidth/leftIcon/rightIcon), `Card` (+variant/padding/hoverable/interactive/bordered), `Chart` (+kind/series), `Dialog` (+footer/size), Box (+full styling-props suite), and many more. Plus iter-37 only generated scaffold.yaml for scaffolds that didn't have one — never re-checked the existing (drifted) react-app one. Re-regenerated all 7 self-contained scaffolds (react-app, landing, dashboard, data-viz, form-app, fullstack, realtime) — each now reflects current TS truth. The 2 inheritance markers (auth-app, ai-app) and the 11 scaffolds without scaffold.yaml are unaffected (no TS to drift against). Per-scaffold drift counts logged in commit. |
| 2026-04-26 | v3.23 | Iteration-42 — TWO related findings extending iter-30's "false 1:1 pairing" pattern: (A) CLAUDE.md's build-pattern step 2 said "read `__fixtures__/drone_natural.tsx`" as universal. Reality: only 7 of 20 scaffolds have that file. The cold-tests across iters 6/8/9/10/18/19/32 all coincidentally tested scaffolds that DO have drone_natural.tsx (or didn't exercise the build flow); the chrome-extension cold-test passed because the agent NAMED the file but didn't try to actually read it. Real coverage: 7 self-contained React scaffolds have drone_natural.tsx + `<scaffold>_patterns.tsx`; 2 inheriting (auth-app, ai-app) have scaffold-specific fixtures with different names (`auth_flow.tsx`, `chat_stream.tsx`); 11 (bespoke-gate / engine / game / gamedev / lighter) have NO `__fixtures__/` dir — contract is README + package.json + src/ structure. Updated build-pattern step 2 with explicit per-tier branching. (B) Iter-38's inheritance-marker in auth-app/ai-app scaffold.yaml said "Scaffold-specific contract lives in __fixtures__/drone_natural.tsx" — but those scaffolds don't have drone_natural.tsx, they have auth_flow.tsx / chat_stream.tsx. Fixed both markers + updated the regen script's fallback path to scan the actual __fixtures__/ dir for the scaffold-specific (non-generic) fixture name and emit the correct path. Future inheriting scaffolds will get the right pointer auto-generated. |
| 2026-04-26 | v3.24 | Iteration-43 — extending iter-42's drone_natural-isn't-universal fix to two more places where the same staleness lurked. Found: (1) "Don't ad-hoc modify scaffolds" section still claimed "Closed-status scaffolds (most React-family) have locked `__fixtures__/drone_natural.tsx`" — same drift as iter 42 caught in build-pattern step 2 (auth-app + ai-app inheriting scaffolds have scaffold-specific fixtures, not drone_natural.tsx). Reframed to enumerate the 9 React-shape closed scaffolds (7 self-contained with drone_natural + 2 inheriting with auth_flow.tsx / chat_stream.tsx). (2) "Where to dig deeper" section claimed `__fixtures__/<scaffold>_patterns.tsx` as a uniform convention. Reality: irregular naming — `data-viz` drops the hyphen (`dataviz_patterns.tsx`), `form-app` drops the `-app` suffix (`form_patterns.tsx`), `react-app`'s pattern fixture is named `landing_dashboard_gallery.tsx` (doesn't follow the `*_patterns.tsx` convention at all). Replaced the placeholder pattern with an explicit per-scaffold enumeration of all 7 actual filenames + a note that 11 scaffolds don't have a patterns fixture. Verified all 7 enumerated paths resolve. |
| 2026-04-26 | v3.25 | Iteration-45 — discovered 10 sub-scaffolds CLAUDE.md was hiding from cold instances. The lighter-scaffold rows (`cli`, `mobile`, `infra`, `training`, `web`) had been treated as single templates since v1; reality is each is a CATEGORY containing concrete sub-templates: `cli/{config-generator, data-processor, file-converter}` (3); `mobile/{chat, notes}` (2); `web/{blog, docs-site, ecommerce}` (3); `infra/{docker-compose}` (1); `training/{finetune-recipe}` (1). All 10 have READMEs with Pitch + Quick Start. Cold instance asked "build me a CLI to process JSON streams" wouldn't have known `cli/data-processor` exists (jq/mlr/csvkit-style stream-processing template) — they'd have seen `cli/` as one thing. Same gap pattern as gamedev's 10 genre subdirs (which CLAUDE.md DID document since v3.1) — the lighter scaffolds had identical structure but never got the same treatment. Replaced 4 lighter-scaffold rows with 10 specific sub-scaffold rows + added a "lighter scaffolds are CATEGORIES not single templates" callout. Updated build-pattern step 2 to point at the specific sub-scaffold's README rather than "ls the dir and hope". |
