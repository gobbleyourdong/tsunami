# Copy this to .env and fill in. The compose file reads .env at build
# and run time; never commit .env (the scaffold's .gitignore excludes it).

POSTGRES_USER=app
POSTGRES_PASSWORD=changeme
POSTGRES_DB=app
NODE_ENV=production
