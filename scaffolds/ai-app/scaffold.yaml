# AUTO-GENERATED — ai-app inherits UI from react-app
# Regenerate with: python3 -m tsunami.scripts.regen_scaffold_yaml ai-app

render_target: dom
frame_cadence: event

inherits_from: react-app  # see scaffolds/react-app/scaffold.yaml for UI components

# Scaffold-specific contract lives in __fixtures__/chat_stream.tsx
# (locks the API surface this scaffold adds on top of react-app's UI).
# Read that fixture FIRST for the locked contract.
