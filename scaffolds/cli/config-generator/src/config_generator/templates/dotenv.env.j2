# Generated by config-generator — edit params, not this file
APP_NAME={{ app.name }}
APP_ENV={{ app.env | default('development') }}
DATABASE_URL={{ database.url }}
SERVER_PORT={{ server.port | default(8080) }}
{% if features is defined -%}
{% for name, enabled in features.items() -%}
FEATURE_{{ name | upper }}={{ 'true' if enabled else 'false' }}
{% endfor -%}
{% endif %}
