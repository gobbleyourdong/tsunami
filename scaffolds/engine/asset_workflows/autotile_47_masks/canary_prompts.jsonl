{"id": "canary_001_full_match", "bitmask": 255, "inputs": ["tileable_terrain/grass_128.png", "tileable_terrain/stone_128.png"], "expected": "all 8 neighbors match, so the tile shows pure grass edge-to-edge — this is an 'interior' tile of a grass region"}
{"id": "canary_002_isolated", "bitmask": 0, "inputs": ["tileable_terrain/grass_128.png", "tileable_terrain/stone_128.png"], "expected": "no neighbors match, so only the central 2x2 of the 4x4 grid shows grass — the 'island' tile surrounded entirely by stone"}
{"id": "canary_003_edge_nes", "bitmask": 21, "inputs": ["tileable_terrain/grass_128.png", "tileable_terrain/stone_128.png"], "expected": "N+E+S edges match (W does not), so grass fills most of the tile but stone shows as a vertical strip on the west side — the 'left edge of a grass region' tile"}
