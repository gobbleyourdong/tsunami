[pytest]
python_files = test_*.py *_test.py *.test.py
# importlib mode lets pytest load files with dots in the name
# (e.g. canary.test.py for scaffold canaries that mirror the TS
# *.test.ts convention) without requiring them to be valid module names.
addopts = --import-mode=importlib
