# TSUNAMI — Docker Configuration
# Models at /app/models, workspace at /app/workspace

model_backend: api
model_name: "Gemma4-E4B"
model_endpoint: "http://localhost:8090"
temperature: 0.7
max_tokens: 8192

eddy_endpoint: "http://localhost:8090"

watcher_enabled: true
watcher_model: "Gemma4-E4B"
watcher_endpoint: "http://localhost:8090"
watcher_interval: 3

workspace_dir: "/app/workspace"
skills_dir: "/app/skills"

tool_timeout: 120
error_escalation_threshold: 3

search_backend: duckduckgo
browser_headless: true
